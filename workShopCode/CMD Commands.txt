STEP 1
======
>python -m venv /myEnv

>/myEnv/Scripts/activate.bat

>python -m pip install --upgrade pip setuptools wheel

>pip install django

>django-admin startproject nonTrivialProject

>cd nonTrivialProject

>python manage.py startapp nonTrivialApp

>pip install djangorestframework

################
paste this into settings.py--->

REST_FRAMEWORK = {
    'DEFAULT_PERMISSION_CLASSES': [],
    'TEST_REQUEST_DEFAULT_FORMAT': 'json'
}
##############

>pip install Pillow   #if you want to deal with images

##############
paste this into settings.py--->

MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

#to be pasted under "STATIC_URL = '/static/'" -->

STATIC_ROOT = os.path.join(BASE_DIR, 'static')


##############

#############
after defining a model and registering it in the admin
#############

>python manage.py makemigrations

>python manage.py migrate

>python manage.py createsuperuser

>python manage.py runserver 9999

>pip freeze > requirements.txt

STEP2
=====
After setting up and wriiting the tests and explaining the minimum code in the view functions to make tests fail then run

>python manage.py test

########### CODE COVERAGE ###################
>pip install coverage

>pip freeze>requirements.txt

>coverage run --source='.' manage.py test

>coverage report



